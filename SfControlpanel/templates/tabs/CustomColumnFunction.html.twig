import React from 'react'

interface Props {
    value: any,
    item: any,
    relSchema?: string,
}

export default function {{ compName }}(props: Props) {
    return (
        <div>{{ compName }}</div>
    )
}
