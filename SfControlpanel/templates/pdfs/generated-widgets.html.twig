import { INaeWidget, WidgetType } from '../../../_generated/v3/utils';

import { Fragment } from 'react';
{% for comp in components %}
import { {{ comp }}Widget } from './buttons/{{ comp }}';
{% endfor %}

export const GeneratedPdfWidgets: INaeWidget[] = [
{% for key, comp in components %}
    {
        schema: "{{ key }}",
        type: WidgetType.viewAfterPdfButton,
        sort: 0,
        options: {},
        comp: {{ comp }}Widget
    },
{% endfor %}
];
