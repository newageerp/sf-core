import React from 'react'

type Props = {
    sourceSchema: string,
    sourceId: number,
    user: number,
}

export default function ElementBookmarkButton(props: Props) {
  return (
    <div>ElementBookmarkButton</div>
  )
}
